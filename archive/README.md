# Archive

Retired-but-kept artifacts. Nothing here is used by the build or runtime.

- `plans/` — planning and audit docs from past work sessions
  (`FIX-PLAN.md`, `SLOP-AUDIT.md`, `AUDIT-2026-07.md`, `IMPLEMENTATION-PLAN.md`) plus the
  `graph_knowledge_fragment.json` graph snapshot.
- `scripts/` — one-off scratch scripts and graphify/codebase-analysis helpers
  (`*.py`, scratch `*.js`).

Safe to delete entirely if you don't need the history. The old pre-Tauri `SolidJS/`
implementation was removed (recoverable from git history).
