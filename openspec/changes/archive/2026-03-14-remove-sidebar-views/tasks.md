## 1. Clean Up Sidebar UI

- [x] 1.1 Remove the "Views" label div from `Sidebar.tsx`.
- [x] 1.2 Remove the three navigation items corresponding to "All Services", "Running", and "Stopped" from `Sidebar.tsx`.

## 2. Clean Up Unused Code

- [x] 2.1 Remove the unused state functions (`runningCount`, `stoppedCount`, `allCount`) from `Sidebar.tsx`.
- [x] 2.2 Remove the `handleFilterClick` function from `Sidebar.tsx`.
- [x] 2.3 Remove unused imports (`currentFilter`, `setCurrentFilter`) from `Sidebar.tsx`.

## 3. Verification

- [x] 3.1 Verify that the Sidebar still renders correctly and the remaining "Actions" and "Settings" sections function as expected.
- [x] 3.2 Verify that the application builds/type-checks successfully.
