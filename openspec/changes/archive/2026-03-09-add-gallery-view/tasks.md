## 1. Core State and Layout

- [x] 1.1 Add a reactive state variable to track the current view mode (`list` or `gallery`).
- [x] 1.2 Implement CSS Grid styling for the `.view-gallery` container and its item cards.

## 2. UI Controls

- [x] 2.1 Add a view toggle button/control to the user interface to switch between views.
- [x] 2.2 Connect the view toggle control to the reactive state.

## 3. Component Updates

- [x] 3.1 Modify the main content container to apply layout classes dynamically based on the state.
- [x] 3.2 Update individual item and folder rendering logic to support the card-based visual style of the gallery.
- [x] 3.3 Ensure expanded folders in the gallery view render their children appropriately within the grid flow.

## 4. Verification

- [x] 4.1 Verify that the UI successfully switches between list and gallery modes.
- [x] 4.2 Verify that folders can be expanded and collapsed within the gallery view without layout breakage.
- [x] 4.3 Ensure the gallery layout is responsive across different window sizes.
